/*#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
	int N;
	cin >> N;
	while (N--) {
		int n;
		cin >> n;
		vector <int>s(n);
		for (int i = 0; i < n; i++) {
			cin >> s[i];
		}
		int count = 0;
		for (int i = 0; i < n; i++) {
			for (int j = 0; j < n - i - 1; j++) {
				if (s[j] > s[j + 1]) {
					swap(s[j], s[j + 1]);
					count++;
				}
			}
		}
		cout << "Optimal train swapping takes " << count << " swaps.\n";
	}
	return 0;
}*/