/*#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
	int N;
	cin >> N;
	while (N--) {
		int n;
		cin >> n;

		vector<int>s(n);
		for (int i = 0; i < n; i++) {
			cin >> s[i];
		}

		sort(s.begin(), s.end());

		int m = s[n / 2];
		long long result = 0;

		for (int i = 0; i < n; i++) {
			result += abs(m - s[i]);
		}

		cout << result <<'\n';
	}
	return 0;
}*/