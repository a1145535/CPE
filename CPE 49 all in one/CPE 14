/*#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
	int S;
	cin >> S;
	while (S--) {
		int N, i;
		double p;
		cin >> N >> p >> i;

		if (p < 1e - 9) {
			cout << "0.0000" << '\n';
			continue;
		}

		double q = 1 - p;

		double a1 = pow(q, i - 1) * p;
		double r = pow(q, N);
		double result = a1 / (1 - r);

		cout << fixed << setprecision(4) << result << '\n';
	}
	return 0;
}*/