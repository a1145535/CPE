/*#include <iostream>
#include <string>
#include <sstream>
#include <vector>
using namespace std;

int main() {
	int x;
	while (cin >> x) {
		cin.ignore();
		vector<int>v;
		string s;
		getline(cin, s);
		stringstream a(s);
		int val;
		while (a >> val) {
			v.push_back(val);
		}
		if (v.size() <= 1) {
			cout << "0" << "\n";
			continue;
		}

		long long ans = 0;
		long long n = v.size() - 1;
		for (int i = 0; i < n; i++) {
			long long d = v[i] * (n - i);
			ans = ans * x + d;
		}
		cout << ans << '\n';
	}
	return 0;
}*/