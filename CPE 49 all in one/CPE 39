/*#include <iostream>
#include <string>
#include <cmath>
using namespace std;

int main() {

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	string cmd;
	while (cin >> cmd && cmd != "0") {
		int odd_sum = 0;
		int even_sum = 0;
		for (int i = 0; i < cmd.length(); i++) {
			int digit = cmd[i] - '0';
			if (i % 2 == 0) {
				even_sum += digit;
			}
			else {
				odd_sum += digit;
			}
		}
		int diff = abs(even_sum - odd_sum);
		if (diff % 11 == 0) {
			cout << cmd << " is a multiple of 11.\n";
		}
		else {
			cout << cmd << " is not a multiple of 11.\n";
		}
	}
	return 0;
}*/