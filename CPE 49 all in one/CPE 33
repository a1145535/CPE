/*#include <iostream>
#include <map>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int N;
	cin >> N;
	map<string, int>m;
	for (int i = 0; i < N; i++) {
		string country;
		cin >> country;
		string name;
		getline(cin, name);
		m[country]++;
	}
	for (auto const& c : m) {
		cout << c.first << " " << c.second << '\n';
	}
	return 0;
}*/