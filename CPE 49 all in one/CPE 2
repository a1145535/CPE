/*#include <iostream>
#include <string>
using namespace std;

int dx[] = { 0, 1, 0, -1 };
int dy[] = { 1, 0, -1, 0 };
char dc[] = { 'N', 'E', 'S', 'W' };

int gd(char c) {
	if (c == 'N')return 0;
	if (c == 'E')return 1;
	if (c == 'S')return 2;
	if (c == 'W')return 3;
	return 0;
}

bool scent[51][51] = { false };

int main() {
	int Mx, My;
	cin >> Mx >> My;

	int x, y;
	char cd;
	while (cin >> x >> y >> cd) {
		string cmd;
		cin >> cmd;
		int d = gd(cd);
		bool lost = false;

		for (char c: cmd) {
			if (c == 'R') {
				d = (d + 1) % 4;
			}
			else if (c == 'L') {
				d = (d + 3) % 4;
			}
			else if (c == 'F') {
				int nx = x + dx[d];
				int ny = y + dy[d];

				if (nx < 0 || nx > Mx || ny < 0 || ny > My) {
					if (!scent[x][y]) {
						scent[x][y] = true;
						lost = true;
						break;
					}
				}
				else {
					x = nx;
					y = ny;
				}
 			}
		}
		cout << x << " " << y << " " << dc[d];
		if (lost) cout << " LOST";
		cout << '\n';
	}
	return 0;
}*/