/*#include <iostream>
#include <vector>
using namespace std;

int main() {
	vector<int>fib;
	fib.push_back(1);
	fib.push_back(2);
	while (true) {
		int next = fib.back() + fib[fib.size() - 2];
		if (next > 100000000)break;
		fib.push_back(next);
	}

	int n;
	cin >> n;
	while (n--) {
		bool start = false;
		int num;
		cin >> num;
		cout << num << " = ";
		for (int i = fib.size() - 1; i >= 0; i--) {
			if (start == false) {
				if (num >= fib[i]) {
					num -= fib[i];
					cout << "1";
					start = true;
				}
			}
			else {
				if (num >= fib[i]) {
					num -= fib[i];
					cout << "1";
				}
				else {
					cout << "0";
				}
			}
		}
		cout << " (fib)\n";
	}
	return 0;
}*/