/*#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int gcd(int a, int b) {
	while (b != 0) {
		a %= b;
		swap(a, b);
	}
	return a;
}

int main() {
	int N;
	int t = 1;
	cin >> N;
	while (N--) {
		cout << "Pair #" << t << ": ";
		string s1, s2;
		cin >> s1 >> s2;
		int n1 = stoi(s1, nullptr, 2);//新東西!!
		int n2 = stoi(s2, nullptr, 2);
		if (gcd(n1, n2) == 1) {
			cout << "Love is not all you need!";
		}
		else {
			cout << "All you need is love!";
		}
		t++;
		cout << '\n';
	}
	return 0;
}*/