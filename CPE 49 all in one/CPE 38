/*#include <iostream>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	string cmd;
	while (cin >> cmd && cmd != "0") {
		int sum = 0;
		for (int i = 0; i < cmd.length(); i++) {
			int cal = cmd[i] - '0';
			sum += cal;
		}
		if (sum % 9 != 0) {
			cout << cmd << " is not a multiple of 9.\n";
		}
		else {
			int degree_count = 1;
			while (sum > 9) {
				int next_sum = 0;
				while (sum > 0) {
					next_sum += sum % 10;
					sum /= 10;
				}
				sum = next_sum;
				degree_count++;
			}
			cout << cmd << " is a multiple of 9 and has 9 degree " << degree_count << '.\n';
		}
	}
	return 0;
}*/